# 저장소 가이드라인

## 기준 데이터와 언어

기본 언어는 한국어입니다. 화면 문구, 일정 데이터, 커밋 메시지, PR 설명, QA 보고서는 특별한 이유가 없으면 한국어로 작성합니다. 여행 일정·장소·식당·체험 정보의 1차 기준 출처는 [제주도 여행계획 관리](chatgpt-conversation://6a79e79f-9d30-83ee-a0af-35618c385723) 대화입니다. 특히 각 날짜의 일정, 기존 약도 교체 상태, 제주 먹거리 우선순위를 그 대화와 일치시킵니다.

대화에 없는 운영시간, 운항, 가격, 날씨처럼 변동 가능한 정보는 게시 전 최신 출처로 확인하고 확인일 또는 출처를 함께 남깁니다. 사용자가 새 지시를 주면 그것이 기존 대화보다 우선합니다.

## 프로젝트 구조

이 프로젝트는 Vite와 React 19 기반의 단일 페이지 제주 여행 가이드입니다. `src/main.tsx`가 `app/page.tsx`의 앱을 마운트합니다. 일정 데이터와 화면 컴포넌트는 현재 `app/page.tsx`에 있으며, 변경 범위가 커질 때만 목적별 모듈로 분리합니다. 공통 스타일은 `app/globals.css`, 배포 이미지 자산은 `public/`에 둡니다. 예: `public/jeju-hero-v1.webp`.

`reports/`에는 QA 기록과 기준 스크린샷이 있고, `.github/workflows/deploy-pages.yml`은 `main`의 `dist/`를 GitHub Pages로 배포합니다. `db/`, `drizzle/`, `worker/`는 현재 Vite 빌드에 포함되지 않는 연동용 골격입니다.

## 개발·빌드·검증

Node.js 22 이상과 pnpm 10을 사용하며 CI와 동일하게 맞춥니다.

```bash
pnpm install             # 잠긴 의존성 설치
pnpm dev                 # 로컬 개발 서버 실행
pnpm build               # 타입 검사 후 dist/ 빌드
pnpm preview             # 프로덕션 빌드 로컬 확인
```

PR 전에는 반드시 `pnpm build`를 실행합니다. 현재 자동 테스트 명령은 없으며 `tests/rendered-html.test.mjs`는 현 Vite 앱에 연결되지 않은 레거시 템플릿 테스트입니다. 데스크톱과 모바일에서 지도 확대·드래그, 일정 전환, 장소 선택, 외부 지도 링크를 직접 확인합니다.

## 작업·배포 흐름

기본 순서는 반드시 **개발 → 테스트 → 커밋**입니다. 변경을 구현한 뒤 적절한 자동 검사와 브라우저 수동 검증을 수행하고, 통과한 변경만 커밋합니다. 커밋 후에는 변경 내용과 검증 결과를 간단히 보고합니다.

`main` 푸시는 GitHub Pages 배포를 자동으로 시작하므로, 배포 가능하다고 판단해도 사용자에게 먼저 배포 승인을 요청합니다. 승인 전에는 원격 푸시나 배포 워크플로 실행을 하지 않습니다. 사용자가 승인하면 푸시한 뒤 GitHub Actions의 빌드·배포 완료 상태와 공개 사이트 접속을 확인해 보고합니다. 사용자가 명시적으로 다른 순서나 즉시 배포를 지시한 경우에만 그 지시를 따릅니다.

## 코드·콘텐츠 작성 규칙

엄격한 TypeScript와 React 함수 컴포넌트를 사용합니다. 기존의 두 칸 들여쓰기, 큰따옴표, 세미콜론, `camelCase`를 따르고 컴포넌트·타입에는 `PascalCase`를 씁니다. 예: `MapZoomControls`, `IndexedPlace`. 새 버튼에는 한국어 `aria-label`을 제공하고, 새 탭 외부 링크에는 `rel="noreferrer"`를 유지합니다. 공개 자산은 GitHub Pages 경로가 유지되도록 `assetUrl()`로 참조합니다.

식당은 날짜별로 `1순위 1곳 + 대기 시 대안 1곳`을 지향합니다. 갈치조림, 우도 점심, 흑돼지·고기국수·보말 요리 등은 기준 대화의 구성과 중복 원칙을 따르며, 변경 시 일정·약도·장소 목록을 함께 갱신합니다.

## 커밋과 PR

최근 이력처럼 변경 결과를 설명하는 짧은 한국어 명령형 제목을 사용합니다. 예: `지도 확대 기능 반영`. PR에는 사용자에게 보이는 변경점, `pnpm build` 결과, 확인한 화면 크기를 적고, 시각·반응형 변경에는 전후 스크린샷을 첨부합니다. `dist/`, 의존성 폴더, 비밀값, 로컬 환경 파일은 커밋하지 않습니다.
